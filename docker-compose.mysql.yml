version: '3.1'

services:
    
    db:
        image: mysql
        ports:
            - 3306:3306
        environment:
            - MYSQL_ROOT_PASSWORD=example
        volumes: 
            - mysqlvol:/var/lib/mysql

volumes:
    mysqlvol:

